[Русская версия](README.ru.md) | [English version](README.md)

# Тестовое задание: Яблочный сад

## Описание

Проект реализует функционал для работы с яблоками в соответствии с тестовым заданием. 

## Технический стек

- Yii2 Advanced Application Template
- PHP 7.4+
- MySQL 5.7+
- Composer
- Twitter Bootstrap 4

## Требования

- PHP 7.4 или выше с расширениями:
  - PDO
  - PDO MySQL
  - JSON
  - cURL
- MySQL 5.7 или выше
- Composer
- Веб-сервер (Apache/Nginx) с поддержкой URL Rewrite

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd test-apple
```

2. Установите зависимости:
```bash
composer install
```

3. Настройте подключение к базе данных в файле `common/config/main-local.php`

4. Примените миграции:
```bash
php yii migrate
```

5. (Опционально) Запустите встроенный веб-сервер:
```bash
php yii serve --docroot="@backend/web"
```

## Функциональность

### Модель Apple

Класс `common\models\Apple` реализует следующую логику:

#### Состояния яблока (статусы):
- 🍏 `STATUS_ON_TREE` (0) - Висит на дереве
- 🍎 `STATUS_ON_GROUND` (1) - Упало/лежит на земле
- 🍏➡️🍂 `STATUS_ROTTEN` (2) - Гнилое яблоко

#### Доступные действия:
- `fallToGround()` - Упасть с дерева на землю
- `eat(int $percent)` - Съесть указанный процент
- `isRotten()` - Проверить, не испортилось ли яблоко
- `updateRottenState()` - Обновить состояние гнилости

#### Атрибуты:
- `color` - Цвет (выбирается случайно из предустановленных)
- `appeared_at` - Время появления (unix timestamp)
- `fell_at` - Время падения (если упало)
- `status` - Текущий статус (0-2)
- `eaten_percent` - Процент съеденного (0-100)
- `size` - Размер яблока (1.0 - целое, уменьшается при откусывании)
- `rotten_at` - Время, когда яблоко испортилось
- `eaten_at` - Время, когда яблоко было полностью съедено

#### Бизнес-правила:
- Яблоко создается со статусом "на дереве"
- Пока яблоко на дереве - оно не портится
- Находясь на дереве, яблоко нельзя съесть
- После падения яблоко может испортиться через 5 часов
- Испорченное яблоко нельзя есть
- При откусывании части яблока уменьшается его размер
- После полного съедания яблоко помечается как съеденное
- Поддерживается несколько цветов яблок с HEX-кодами для отображения

## Использование

### Генерация яблок
На главной странице можно сгенерировать случайное количество яблок. Каждое новое яблоко получает:
- Случайный цвет из предустановленных
- Случайное время появления (в пределах 30 дней)
- Статус "на дереве"
- Размер 1.0 (целое яблоко)

### Управление яблоками

#### Доступные действия:
- **Для висящих яблок (🍏):**
  - Уронить на землю (кнопка "Уронить")

- **Для упавших яблок (🍎):**
  - Откусить часть (форма ввода процента + кнопка "Откусить")
  - Автоматическая проверка на гниение (после 5 часов на земле)

- **Для гнилых яблок (🍏➡️🍂):**
  - Только просмотр, никаких действий не доступно

- **Для всех яблок:**
  - Просмотр детальной информации
  - Удаление (если полностью съедено)

## Примеры использования

### Создание яблока
```php
// Создаем новое яблоко (цвет выберется случайно)
$apple = new Apple();
$apple->save();

echo $apple->color; // Например: 'green'
echo $apple->getStatusLabel(); // 'Висит на дереве'
```

### Взаимодействие с яблоком
```php
// Пытаемся съесть яблоко на дереве
try {
    $apple->eat(50); // Выбросит исключение: 'Яблоко уже не на дереве.'
} catch (\DomainException $e) {
    echo $e->getMessage();
}

// Сбрасываем яблоко на землю
$apple->fallToGround();

echo $apple->getStatusLabel(); // 'Лежит на земле'

// Откусываем четверть яблока
$apple->eat(25);
echo $apple->eaten_percent; // 25

echo $apple->size; // 0.75 (осталось 75% от размера)

// Проверяем, не испортилось ли яблоко
if ($apple->isRotten()) {
    echo 'Яблоко испортилось!';
}
```

### Генерация случайного яблока
```php
// Создаем яблоко со случайным цветом и временем появления
$apple = new Apple();
$apple->color = Apple::randomColor();
$apple->appeared_at = Apple::randomAppearedAt();
$apple->save();
```

## Структура проекта

```
common/
    models/         # Общие модели
    config/         # Общие конфигурации
    mail/           # Шаблоны писем
    widgets/        # Виджеты

backend/
    controllers/    # Контроллеры backend
        TesttaskController.php  # Управление яблоками
    models/         # Модели backend
    views/          # Представления backend
        testtask/   # Шаблоны для работы с яблоками
            _apple_item.php  # Шаблон карточки яблока
            _form.php        # Форма создания/редактирования
            index.php        # Список яблок
    web/            # Веб-ресурсы

console/
    controllers/    # Консольные команды
    migrations/     # Миграции базы данных
        m251204_174432_create_apple_table.php  # Миграция таблицы яблок

tests/              # Тесты

environments/       # Конфигурации окружений
```

## Тестирование

Для запуска тестов выполните:

```bash
./vendor/bin/codecept run
```

## Лицензия

MIT

## Технические детали реализации

### База данных
- Используется таблица `apple` со следующей структурой:
  ```sql
  CREATE TABLE `apple` (
    `id` int(11) NOT NULL AUTO_INCREMENT,
    `color` varchar(32) NOT NULL,
    `appeared_at` int(11) NOT NULL,
    `fell_at` int(11) DEFAULT NULL,
    `status` smallint(6) NOT NULL DEFAULT 0,
    `eaten_percent` int(11) NOT NULL DEFAULT 0,
    `size` decimal(3,2) NOT NULL DEFAULT 1.00,
    `rotten_at` int(11) DEFAULT NULL,
    `eaten_at` int(11) DEFAULT NULL,
    `created_at` int(11) NOT NULL,
    `updated_at` int(11) NOT NULL,
    PRIMARY KEY (`id`)
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
  ```

### Ключевые особенности
- Валидация входных данных
- Обработка ошибок через исключения
- Логика гниения реализована через метод `updateRottenState()`
- Поддержка нескольких цветов яблок с HEX-кодами
- Локализованные статусы яблок
- Логирование важных событий

### Миграции

Для настройки базы данных выполните:
```bash
# Применить все миграции
php yii migrate

# Откатить последнюю миграцию
php yii migrate/down

# Показать список миграций
php yii migrate/history
```

Миграция `m251204_174432_create_apple_table.php` создает таблицу `apple` со всеми необходимыми полями.

### Настройки

Основные настройки приложения:

1. **Общие настройки** (`common/config/main.php`):
   - Настройки компонентов
   - Параметры приложения
   - Настройки алиасов

2. **Backend** (`backend/config/main.php`):
   - Конфигурация контроллеров
   - Настройки URL-менеджера
   - Компоненты приложения

3. **Консоль** (`console/config/main.php`):
   - Настройки консольных команд
   - Компоненты консольного приложения

4. **Параметры** (`common/config/params.php`):
   - Количество яблок при генерации
   - Время до гниения (5 часов)
   - Другие настраиваемые параметры

5. **Локальные настройки** (не в репозитории):
   - `common/config/main-local.php`
   - `backend/config/main-local.php`
   - `console/config/main-local.php`
   - `common/config/params-local.php`

## Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой фичи (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add some amazing feature'`)
4. Отправьте изменения в репозиторий (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

## Автор

[Ваше имя] - [ваш email]

## Лицензия

MIT

## Благодарности

- Команде Yii2 за отличный фреймворк
- Сообществу разработчиков с открытым исходным кодом
- Всем, кто вносит вклад в развитие open-source
    controllers/         contains Web controller classes
    models/              contains frontend-specific model classes
    runtime/             contains files generated during runtime
    tests/               contains tests for frontend application
    views/               contains view files for the Web application
    web/                 contains the entry script and Web resources
    widgets/             contains frontend widgets
vendor/                  contains dependent 3rd-party packages
environments/            contains environment-based overrides
```
